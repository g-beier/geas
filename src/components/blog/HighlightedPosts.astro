---
import Grid from '~/components/blog/Grid.astro';

import { findPostsByIds } from '~/utils/posts';

const ids = ['historia-do-geas', 'de-volta-a-nossa-natureza'];
const posts = await findPostsByIds(ids);
---

<section class="px-4 py-16 mx-auto max-w-6xl lg:py-20" id="posts">
	<div class="flex flex-col mb-6 lg:justify-between lg:flex-row md:mb-8">
		<h2 class="max-w-lg mb-2 text-3xl font-bold tracking-tight sm:text-4xl sm:leading-none lg:mb-5 group font-heading">
			<span class="inline-block mb-1 sm:mb-4">Acompanhe as atividades<br class="hidden md:block" /> no nosso Blog</span>
		</h2>

		<p class="text-gray-700 dark:text-slate-400 lg:text-sm lg:max-w-md">
			Postamos no blog sobre os eventos e atividades do Grupo. As fotos estarão nas redes sociais, mas aqui você pode
			acompanhar um pouco do que fazemos no Albert.
		</p>
	</div>

	<Grid posts={posts} />
</section>
